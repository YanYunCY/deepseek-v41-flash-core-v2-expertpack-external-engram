[project]
name = "deepseek-v41-flash-external-engram"
version = "0.1.0"
description = "Core v2, ExpertPack, external Engram deployment, benchmark, and local adapter tooling for DeepSeek-V4.1-Flash"
requires-python = ">=3.10"
dependencies = []

[tool.dsv41]
tests-use-standard-library = true
model-artifacts-host = "https://modelscope.cn/"

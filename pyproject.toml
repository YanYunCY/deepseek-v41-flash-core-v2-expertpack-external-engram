[project]
name = "dsv41-deployment-toolkit"
version = "0.1.0"
description = "Deployment, benchmark, and local adapter tooling for a community DeepSeek-V4.1-Flash AMD/ROCm deployment"
requires-python = ">=3.10"
dependencies = []

[tool.dsv41]
tests-use-standard-library = true
model-artifacts-host = "https://modelscope.cn/"
